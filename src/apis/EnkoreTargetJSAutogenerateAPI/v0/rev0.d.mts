import type {EnkoreAutogeneratedFile} from "#~src/export/__star_export.mts"

export type Definition = {
	generateWrapperAndFactoryFile: (
		options: {
			source: string
			exportName: string
			destination: string
		}
	) => EnkoreAutogeneratedFile[]

	generateFactoryFile: (
		options: {
			source: string
			exportName: string
			destination: string
		}
	) => EnkoreAutogeneratedFile[]
}

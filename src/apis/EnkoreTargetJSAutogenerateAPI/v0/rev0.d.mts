import type {EnkoreAutogeneratedFile} from "#~src/export/__star_export.mts"

export type Definition = {
	generateWrapperAndFactoryFiles: (
		options: {
			source: string
			exportName: string
			destination: string
		}
	) => EnkoreAutogeneratedFile[]

	generateFactoryFile: (
		options: {
			source: string
			exportName: string
			destination: string
		}
	) => EnkoreAutogeneratedFile[]
}

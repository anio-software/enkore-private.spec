import type {
	EnkoreTargetJSNone,
	EnkoreTargetJSNode,
	EnkoreTargetJSWeb,
	EnkoreTargetWeb,
	EnkoreTargetC,
	EnkoreAutogeneratedFile
} from "#~src/export/__star_export.mts"

export type Definition = {
	target: EnkoreTargetJSNone |
	        EnkoreTargetJSNode |
	        EnkoreTargetJSWeb  |
	        EnkoreTargetWeb    |
	        EnkoreTargetC

	autogeneratedFiles?: EnkoreAutogeneratedFile[]

	buildConstants?: Record<string, string>
}
